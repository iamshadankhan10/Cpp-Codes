#include <iostream>
#include <climits>
using namespace std;

int main(){

    int a[] = {-1, -2, -3, -4, -5, -6, -7};
    int n = sizeof(a) / sizeof(int);
    int sum=0;
    int ans=INT16_MIN;

    for (int i=0; i<n; i++){
        
    }

    return 0;
}